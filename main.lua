function love.load()
    -- Load the gamestate library (call once at the beginning)
end

function love.update(dt)
    -- Update the gamestate library (call every frame)
end

function love.draw()
    -- Draw the gamestate library (call every frame)
end

function love.keypressed(key)
    -- Pass the keypress to the gamestate library (call for every keypress)
end